# Triage: issues #145, #146, #147, #148

## Summary

Reviewed four filed issues (#145, #146, #147, #148) against the actual code in the files they cite. In each case the described behavior does not exist in the referenced modules. Findings are detailed below; this note collects them for reference and issue resolution, following the precedent established in `docs/triage-308-309-310-311.md`.

## #145 — `lib/phase-host-error.ts` & `diagnose-env.ts`: DISTRIBUTOR_SECRET leak claim

- **Cited range `:12-34` reviewed in full**: `lib/phase-host-error.ts` is a 29-line utility that extracts Soroban contract error codes (`HostError: Error(Contract, #N)`) via regex and humanizes them into localized error strings. It does not import, reference, or access `DISTRIBUTOR_SECRET` or any private key material.
- **`diagnose-env.ts` review**: Lines 93–96 only check whether `process.env.FAUCET_DISTRIBUTOR_SECRET_KEY` is defined and logs a status confirmation (`✅ FAUCET_DISTRIBUTOR_SECRET_KEY configurado`). It never logs or exposes the secret key string. `lib/phase-host-error.ts` is not used in `diagnose-env.ts`.

## #146 — `lib/metadata-migration.ts`: 10M rows lore-versioning data loss claim

- **Cited range `:12-34` reviewed in full**: Defines `CURRENT_METADATA_VERSION = 2` and Zod schemas (`NftMetadataV1Schema`). The file is a pure in-memory validator and migration utility between NFT metadata v1 and v2 formats.
- **Lore-versioning & Docs isolation**: `lib/metadata-migration.ts` has zero connection to `lore-versioning` or `project-docs-content`. It never executes destructive mutations on storage unless explicitly invoked in batch scripts with `--apply`. Furthermore, PHASE is a testnet prototype using lightweight JSON files in `.data/`; it does not manage or store "10M rows".

## #147 — `lib/story-arc-continuity.ts`: checkArc never called claim

- **Cited range `:18-42` reviewed in full**: Defines configuration helpers, Zod validation schemas, and Gemini model identifiers for the AI story-arc continuity check (`phase-107`).
- **Actual invocation**: The function is named `checkNarrativeContinuity()` (not `checkArc`), and it is actively imported and called in `app/api/narrator/route.ts` (line 131) during narrative generation when `phase-107` is enabled. World canon does not diverge across "100k Tokens".

## #148 — `lib/project-docs-content.ts`: XSS & __proto__ pollution claim

- **Cited range `:12-34` reviewed in full**: Declares typed data structures and bilingual documentation copy for the `/docs` route.
- **No `dangerouslySetInnerHTML`**: Searching the codebase confirms 0 occurrences of `dangerouslySetInnerHTML` in `lib/project-docs-content.ts` and `app/docs/page.tsx`. All content is rendered through standard React JSX primitives (`<p>`, `<ul>`, `<li>`, `<table>`).
- **No `__proto__` pollution**: There are 0 occurrences of `__proto__` in `lib/project-docs-content.ts` or `lib/phase-copy.ts`.

## Common Thread

All four issue bodies share the exact same hallucinated Description/Why/Task template previously identified in `docs/triage-308-309-310-311.md`:
- Proposing Yjs CRDT + `WebsocketProvider` live collaborative editing across 30 files.
- Proposing migrating an imaginary `signal-store JSON` to a Postgres `signals(id PK, version INT, vector_clock JSONB, body TEXT, updated_at)` table.
- Proposing `signal-detail-client.tsx` and 50-concurrent-edit tests.
- Repeating identical citations to `lib/phase-contract-defaults.ts:12-34`, `lib/phaser-liq-sac-warn.ts:12-34`, `lib/toast-queue.ts:12-34`, `lib/og-design-tokens.ts:12-34`, and `app/api/cron/faucet-health/route.ts:11-22`.

None of these concepts, tables, or client files exist in the PHASE project architecture.

## Recommendation

Close issues #145, #146, #147, and #148 as **not reproducible / invalid**, consistent with the disposition of #308–#311.
